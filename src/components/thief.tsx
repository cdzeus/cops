import clsx from "clsx";
import { useContext, useState } from "react";

interface Props {
  className?: string;
  isActive?: boolean;
  action?: () => void;
}

export const Thief = () => {
  return <div>Hello</div>;
};
